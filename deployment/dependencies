
adduser bloatit
set default group to www-data: in /etc/passwd field 4 to lighttpd group

set the rights:
find . -type f -execdir chmod 440 {} \;
find . -type d -execdir chmod 710 {} \;
chown bloatit:www-data * -R # penser au fichier qui commencent par ."

mkdir -p $SHARE_DIT/log
mkdir upload
git / gitignore
ssh
rsync


postgresql
lighttpd
java
