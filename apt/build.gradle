
task linkGeneratedJar(type: Exec, dependsOn: jar){
    // workingDir = file('.')
    originJar = 'build/libs/apt-' + version + '.jar'
    cibleJar = 'build/libs/apt.jar'
    commandLine = [ 'ln', '-s', originJar, cibleJar ]
}
build.dependsOn 'linkGeneratedJar'
